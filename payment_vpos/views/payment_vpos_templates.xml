<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="vpos_iframe">
        <iframe t-att-src="iframe_url" width="100%" height="350" style="border:none;"></iframe>

        <!-- 
            no se porque no anda el layout
        <t t-call="website.layout">
            <div>
                Hola aaa
                <!- -
                - ->
            </div>
        </t>
    -->
    </template>

    <template id="vpos_error">
        <div>
            Su pago no pudo ser procesado.
        </div>
    </template>
</odoo>
